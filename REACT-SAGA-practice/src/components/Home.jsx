import React from "react";
import { useDispatch, useSelector } from "react-redux";

const Home = () => {
  const dispatch = useDispatch();
//   const {product} = useSelector((state) => console.log(state.productReducer));
//   console.log(product);
  return <div>home</div>;
};

export default Home;
